export const Roles = Object.freeze({
	dev: 'dev',
	admin: 'admin',
	super_admin: 'super_admin'
});

export const RolesText = Object.freeze({
	dev: 'Desarrollador',
	admin: 'Administrador',
	super_admin: 'Super Administrador'
});

export const StateContract = Object.freeze({
	valid: 'valid',
	invalid: 'invalid'
});
